name: Build APK

on:
  push:
    branches: [ main, principal ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup App
        run: |
          npm install
          mkdir -p www
          cp -r . www/ || true
          rm -rf android capacitor.config.*
          npx cap init BibliaApp com.biblia.app --web-dir www
          npx cap add android

      - name: FORÇAR JAVA 17 (CORREÇÃO FINAL)
        run: |
          cd android
          # Força o Gradle a usar Java 17
          echo "org.gradle.java.home=${JAVA_HOME}" >> gradle.properties
          
          # Entra nos arquivos de configuração e troca 1.8 ou 11 por 17 na marra
          find . -name "build.gradle" -exec sed -i 's/JavaVersion.VERSION_1_8/JavaVersion.VERSION_17/g' {} +
          find . -name "build.gradle" -exec sed -i 's/JavaVersion.VERSION_11/JavaVersion.VERSION_17/g' {} +
          
          chmod +x gradlew

      - name: Gerar APK
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-biblia-final
          path: android/app/build/outputs/apk/debug/app-debug.apk
          
