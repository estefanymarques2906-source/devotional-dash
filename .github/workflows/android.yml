name: Gerar APK Direto
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4 # Atualizei para v4
        with:
          node-version: 22 # Mudei de 20 para 22 para corrigir o erro da sua foto

      - name: Instalar Capacitor e Criar Pasta
        run: |
          npm install @capacitor/cli@latest @capacitor/core@latest @capacitor/android@latest
          mkdir -p dist
          echo '<html><body>App</body></html>' > dist/index.html
          
      - name: Configurar Android
        run: |
          npx cap add android || true
          npx cap copy android

      - name: Gerar APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-final
          path: android/app/build/outputs/apk/debug/app-debug.apk
          
